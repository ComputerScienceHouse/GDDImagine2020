using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Specials : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void DragonAbility()
    {
        //need to decide what fireball does.
        //large round projectile.
    }

    void KnigtAbility()
    {
        //the knight doesn't collide with other game objects until
        //it enters and exits an object.

        //make knight not able to collide.
        //check for when entering and exiting.
        //update collision.
    }

    void WitchAbility()
    {
        //need to decide what purple beam does.
        //horizontal column straight forward.
    }

    void WizardAbility()
    {
        //need to decide what shock rune does
        //probably spreads out.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
